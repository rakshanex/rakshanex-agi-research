# candidate_locked.json is R1's INPUT (locked candidate data to be scored) but its per-record fields reveal
# the aggregate result. R1's runner NEEDS it. If you want a blind R1, score independently from the benchmark;
# otherwise run analyze_r1.py first and treat this as post-run evidence. Not deleted (required input + evidence).
