# EXTERNAL REPLICATION PACKAGE — RAKSHANEX AGI / Selective-Prediction Research
Self-contained deterministic replication of the strongest claims (R1 selective prediction, R2 search-limited
boundary, R3 evaluation invariance). CPU-only, stdlib-only, no API, no secrets. See REPLICATION_GUIDE.md.
Independence: this package enables EXTERNAL replication; runs performed by the original operator are
INTERNAL_CLEAN_ROOM only. R4 (7B/14B) is optional + needs a GPU (free Kaggle). No result numbers leaked here.
