# PROTOCOL_DEVIATIONS
1. R2/R3 runner output paths changed from original project-relative to package-relative (raw/*.json).
   REASON: make standalone. NO scientific change (same generator/solver/seeds/verifier). Logged.
2. No other deviations. Benchmark, seeds, scoring unchanged.
